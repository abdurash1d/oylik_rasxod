# Copy to .env.deploy, fill in values, then: source .env.deploy && ./scripts/deploy_railway.sh
# Do not commit .env.deploy (contains secrets).

export BOT_TOKEN=
export OWNER_TELEGRAM_ID=
export RAILWAY_TOKEN=

# Optional:
# export PROJECT_NAME=rasxot-bot
# export SERVICE_NAME=api
